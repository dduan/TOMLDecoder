# ``TOMLDecoder``

Swift library for decoding TOML (Tom's Obvious Minimal Language).
`TOMLDecoder` enables working with TOML documents with Swift's Codable types.
And it provides fast, type-safe TOML parsing, and deserialization.
`TOMLDecoder` implements the TOML 1.0 spec.

## Topics
